1) setup.m inicializuje parametry a homogenni transformacni matice
2) DGM.m, IGM.m, POKU.m, IOKU.m a generator_trajektorie.m jsou pouze skici, skutečné verze těchto funkcí jsou v simulink solutionech jako Matlab funkce
3) DGM_test_sim.slx testuje shodu manipulátoru s IGM/DGM a taky slouží jako hřbitov pro testy
4) singularni_polohy.m vykresluje hezky obrazky
5) sampleQnorm.m vzorkuje a vykresluje ukol 6 
